[project]
name = "opencodereview-workspace"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = [
    "opencodereview[tools]",
]

[tool.uv.sources]
opencodereview = { path = "python", editable = true }
